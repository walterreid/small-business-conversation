flask==2.3.3
flask-cors==4.0.0
python-dotenv==1.0.0
openai>=1.6.1
gunicorn==21.2.0
pandas>=2.0.0  # Optional: For SMB data loading (graceful fallback if not available)
openpyxl>=3.1.0  # Optional: For Excel reading (graceful fallback if not available)
